import dbConnect from '@/db/dbConfig'
import MemberModel from '@/models/member'
import {CurrentProfile} from "@/lib/current-profile"



export const MembersData = (serverId:string)=>{

    console.log(`serverId from member function ${serverId}`);
    return serverId;
    // try{

    // } catch(error){

    // }
}